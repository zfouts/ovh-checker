# OVH Checker Configuration
# Copy this file to .env and customize for your deployment
#
# ⚠️  SECURITY WARNING: Do not commit your actual .env file to version control!
# ⚠️  All values marked REQUIRED must be set before starting the application!
#
# To generate secure values:
#   Password: openssl rand -base64 24
#   JWT Secret: openssl rand -base64 32

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
POSTGRES_USER=ovhchecker
# REQUIRED: Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=
POSTGRES_DB=ovhchecker

# REQUIRED: Full database connection string
# For docker-compose, use postgres as hostname:
DATABASE_URL=postgresql://ovhchecker:YOUR_PASSWORD_HERE@postgres:5432/ovhchecker
# For local development, use localhost:
# DATABASE_URL=postgresql://ovhchecker:YOUR_PASSWORD_HERE@localhost:5432/ovhchecker

# =============================================================================
# JWT AUTHENTICATION (REQUIRED)
# =============================================================================
# REQUIRED: Generate with: openssl rand -base64 32
# Application will NOT start without this!
JWT_SECRET=

# Token expiration (optional, these are the defaults)
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# REGISTRATION & SECURITY
# =============================================================================
# Set to "false" to disable public registration (admin can still create users)
ALLOW_REGISTRATION=true

# CORS - Use specific origins in production, not "*"
# Multiple origins: CORS_ORIGINS=https://app.example.com,https://admin.example.com
CORS_ORIGINS=https://your-domain.com

# =============================================================================
# API CONFIGURATION
# =============================================================================
API_HOST=0.0.0.0
API_PORT=8000

# =============================================================================
# CHECKER CONFIGURATION
# =============================================================================
CHECK_INTERVAL_SECONDS=120
NOTIFICATION_THRESHOLD_MINUTES=60
# For distributed checkers, set SUBSIDIARY (US, FR, DE, etc.)
# SUBSIDIARY=US
# AGENT_ID=checker-us

# =============================================================================
# INITIAL ADMIN (Optional - only used on first startup)
# =============================================================================
# ADMIN_EMAIL=admin@example.com
# ADMIN_USERNAME=admin
